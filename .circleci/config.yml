version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.10

    working_directory: ~/project

    steps:
      - checkout

      - run:
          name: Upgrade pip
          command: python -m pip install --upgrade pip

      - run:
          name: Install dependencies
          command: |
            if [ -f requirements.txt ]; then
              pip install -r requirements.txt
            fi
            pip install pytest

      - run:
          name: Verify installation
          command: |
            python --version
            pip list

      - run:
          name: Run tests
          command: python -m pytest


workflows:
  build_and_test:
    jobs:
<<<<<<< HEAD
      - build
=======
      - build
>>>>>>> 4be61994ea1286a73e8ed717b99aa76e5dbac45f
